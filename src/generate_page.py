from markdown_to_htmlnode import extract_title

def generate_page(from_path, template_path, dest_path):
    from_path = None
    template_path = None
    dest_path = None

    print(f"Generating page from {from_path} to {dest_path} using {template_path}.")
